import React from 'react';

const teacher = 'PENGUIN';

const StudentViewRumble = (): React.ReactElement => {
  return (
    <div>
      <h1>Get ready to write! Mr.{teacher} will start the rumble shortly.</h1>
    </div>
  );
};

export default StudentViewRumble;
